name: aarch64

# Manual-only end-to-end test on a GitHub-hosted aarch64 runner.
on:
  workflow_dispatch:

jobs:
  e2e:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/e2e-test
        with:
          arch: aarch64
