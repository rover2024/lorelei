name: aarch64

# Manual-only end-to-end test on the aarch64 self-hosted runner. Never runs on
# push; trigger it from the Actions tab ("Run workflow").
on:
  workflow_dispatch:

jobs:
  e2e:
    uses: ./.github/workflows/_ci-shared.yml
    with:
      arch: aarch64
